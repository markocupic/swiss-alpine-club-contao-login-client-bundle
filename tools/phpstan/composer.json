{
    "description": "Isolated PHPStan installation. The classes under analysis are autoloaded from the bundle's own vendor directory, see phpstan.neon.dist.",
    "require": {
        "phpstan/phpstan": "^2.1"
    },
    "config": {
        "allow-plugins": {
            "contao-components/installer": false,
            "contao/manager-plugin": false,
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
